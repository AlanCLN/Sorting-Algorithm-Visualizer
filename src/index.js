const Bar = require("./scripts/classes/bar")

document.addEventListener('DOMContentLoaded', () => {

    // const graph = document.getElementById("bar-container");
    // const newBar = new Bar(80, graph);
    

})